//间接更新，可做异步操作和逻辑代码
import {login} from '@/api'


export default {
	getUers: async function ({commit, state}) {
		// const {user,token}=state;
		// 	//异步请求获取数据
		// const useData = await login({commit})
		// //提交给mutation
		// if (user == useData.user){
		// 	commit('login',useData)
		// }
	}
}